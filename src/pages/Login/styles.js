const getStyles = () => {
  const styles = {
    mainBody: {
      display: "flex",
      justifyContent: "center",
      alignItems: "center",
      backgroundColor: "black",
    },
  };
  return styles;
};

export default getStyles;
